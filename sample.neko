// NekoDSL sample shader — vertex + fragment pair with interface variables

@vertex
const vertex_main := (in_pos : vec4) $ neko_position : vec4 {
    return in_pos;
}

@fragment
const fragment_main := () $ neko_color : vec4 {
    return vec4(1.0, 0.0, 0.5, 1.0);
}
